using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using VideoUploaderDAO.Impl;
using VideoUploaderModel;

namespace VideoUploaderTest.Impl
{
    class DroitRepositoryTest
    {
 /*       static void Main(string[] args)
        {
            try
            {
                DroitGroupeRepository folderRT = new DroitGroupeRepository();
                var itm = new VUDroit
                {
                    Admin = true
                };
                //folderRT.Create(itm);
                //grpRT.Delete(grp);
                //grpRT.DeleteAll();
                EntitySearch entitySearch = new EntitySearch(itm, "", "");
                List<VUDroit> folderResult = folderRT.FindByCriteria(entitySearch);

                Console.WriteLine();

            }
            catch (Exception ex)
            {
                Console.WriteLine(ex.Message);
            }

        }
        */
    }
}
