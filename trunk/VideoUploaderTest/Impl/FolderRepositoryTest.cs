using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using VideoUploaderDAO.Impl;
using VideoUploaderModel;

namespace VideoUploaderTest.Impl
{
    class FolderRepositoryTest
    {
/*               static void Main(string[] args)
                {
                    try
                    {
                        FolderRepository folderRT = new FolderRepository();
                        var itm = new VUFolder
                        {
                            Nom = "folder01",
                            DateCreation = Convert.ToDateTime("2012-02-25"),
                            DateMAJ = DateTime.Now,
                            UserIdUser = 1,
                            VUDroitElementIdVUDroitElement = 1
                        };
                        //folderRT.Create(itm);
                        //grpRT.Delete(grp);
                        //grpRT.DeleteAll();
                        EntitySearch entitySearch = new EntitySearch(itm, "", "");
                        List<VUFolder> folderResult = folderRT.FindByCriteria(entitySearch);

                        Console.WriteLine();

                    }
                    catch (Exception ex)
                    {
                        Console.WriteLine(ex.Message);
                    }

                }
  */              

    }
}
