using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using VideoUploaderDAO.Impl;
using VideoUploaderModel;

namespace VideoUploaderTest.Impl
{
    class CommentRepositoryTest
    {
                   /*  static void Main(string[] args)
                        {
                            try
                            {
                                CommentRepository folderRT = new CommentRepository();
                                var itm = new VUComment
                                {
                                    Contenu = "commentaire01",
                                    ItemIdElement = 1,
                                    UserIdUser = 1
                                };
                                //folderRT.Create(itm);
                                //grpRT.Delete(grp);
                                //grpRT.DeleteAll();
                                EntitySearch entitySearch = new EntitySearch(itm, "", "");
                                List<VUComment> folderResult = folderRT.FindByCriteria(entitySearch);

                                Console.WriteLine();

                            }
                            catch (Exception ex)
                            {
                                Console.WriteLine(ex.Message);
                            }

                        }*/
                        

    }
}
