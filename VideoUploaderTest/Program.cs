using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using VideoUploaderModel;
using VideoUploader;
using VideoUploaderDAO.Impl;

namespace VideoUploaderTest
{
    class Program
    {
/*        static void Main(string[] args)
        {


            var user = new VUUser
            {
                IdUser = 2,
                Login = "massinovicth",
                Nom = "sekhi",
                Prenom = "massinissa",
                GroupeIdGroupe = 1,
                DateCreation = Convert.ToDateTime("2012-02-25"),
                DateMAJ = DateTime.Now,
                Password = "pass"
            };
            //userTest.Create(user);
            UserRepository userTest = new UserRepository();
            var userVide = new VUUser();
            var userSearch = new VUUser
            {
                Login = "RINGO",
                Nom = "SEKHI",
                GroupeIdGroupe = 1
            };
            EntitySearch entitySearch = new EntitySearch(userSearch, "2012-02-24", "");
            List<VUUser> userfind = userTest.FindByCriteria(entitySearch);
            foreach (VUUser result in userfind)
            {
                Console.WriteLine("User Last Name: {0}; First Name: {1} ====== IdUser :: {2} ",
                        result.Nom, result.Prenom, result.IdUser);
            }            
            ItemRepository itemRepository = new ItemRepository();
            List<VUItem> listElement = itemRepository.FindByCriteria(null);
            Console.ReadKey();
    }*/
    }
}
